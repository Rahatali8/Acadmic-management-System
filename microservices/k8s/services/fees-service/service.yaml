apiVersion: v1
kind: Service
metadata:
  name: fees-service
  namespace: ams-production
spec:
  selector:
    app: fees-service
  ports:
    - protocol: TCP
      port: 8008
      targetPort: 8008
  type: ClusterIP
