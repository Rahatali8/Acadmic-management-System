apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-service
  namespace: ams-production
  labels:
    app: support-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: support-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: support-service
    spec:
      containers:
        - name: support-service
          image: registry.example.com/ams/support-serviceatest
          imagePullPolicy: Always
          ports:
            - containerPort: 8011
          envFrom:
            - configMapRef:
                name: support-service-config
            - secretRef:
                name: support-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health/
              port: 8011
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/
              port: 8011
            initialDelaySeconds: 15
            periodSeconds: 10
