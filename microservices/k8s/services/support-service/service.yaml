apiVersion: v1
kind: Service
metadata:
  name: support-service
  namespace: ams-production
spec:
  selector:
    app: support-service
  ports:
    - protocol: TCP
      port: 8011
      targetPort: 8011
  type: ClusterIP
