apiVersion: v1
kind: Service
metadata:
  name: staff-service
  namespace: ams-production
spec:
  selector:
    app: staff-service
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004
  type: ClusterIP
