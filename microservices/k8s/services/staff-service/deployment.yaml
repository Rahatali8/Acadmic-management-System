apiVersion: apps/v1
kind: Deployment
metadata:
  name: staff-service
  namespace: ams-production
  labels:
    app: staff-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: staff-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: staff-service
    spec:
      containers:
        - name: staff-service
          image: registry.example.com/ams/staff-serviceatest
          imagePullPolicy: Always
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: staff-service-config
            - secretRef:
                name: staff-service-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health/
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 10
