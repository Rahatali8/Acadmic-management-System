apiVersion: apps/v1
kind: Deployment
metadata:
  name: org-service
  namespace: ams-production
  labels:
    app: org-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: org-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: org-service
    spec:
      containers:
        - name: org-service
          image: registry.example.com/ams/org-serviceatest
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: org-service-config
            - secretRef:
                name: org-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health/
              port: 8002
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 10
