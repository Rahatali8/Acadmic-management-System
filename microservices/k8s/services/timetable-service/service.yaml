apiVersion: v1
kind: Service
metadata:
  name: timetable-service
  namespace: ams-production
spec:
  selector:
    app: timetable-service
  ports:
    - protocol: TCP
      port: 8009
      targetPort: 8009
  type: ClusterIP
