apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-service
  namespace: ams-production
  labels:
    app: attendance-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: attendance-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: attendance-service
    spec:
      containers:
        - name: attendance-service
          image: registry.example.com/ams/attendance-serviceatest
          imagePullPolicy: Always
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: attendance-service-config
            - secretRef:
                name: attendance-service-secret
          resources:
            requests:
              cpu: "300m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health/
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 10
