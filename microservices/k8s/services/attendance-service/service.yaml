apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: ams-production
spec:
  selector:
    app: attendance-service
  ports:
    - protocol: TCP
      port: 8006
      targetPort: 8006
  type: ClusterIP
