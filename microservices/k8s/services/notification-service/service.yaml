apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ams-production
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8010
      targetPort: 8010
  type: ClusterIP
